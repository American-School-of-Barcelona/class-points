# Class-points backend

## Setup

It's a rust app, not rocket science. You need to make a `.env` file with the following fields:

```env
DB=database.db
ADMIN_PASSWORD=password
ADMIN_EMAIL=admin@asbarcelona.com
GMAIL_USER=sender@gmail.com
GMAIL_PASS=password
```
